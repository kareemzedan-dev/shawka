# Copy to secrets.properties at project root (gitignored) — Android key only
# iOS: ios/Flutter/Secrets.xcconfig with GMS_API_KEY=...
# Or run: scripts/configure-google-maps.ps1 with GOOGLE_MAPS_API_KEY_ANDROID / _IOS

google.maps.apiKey=
